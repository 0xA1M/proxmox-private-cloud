# Defines the minecraft host group with a single VM target.
[minecraft]                                         # Host group used by deploy-minecraft.yml
mc-vm ansible_host=10.0.0.115 ansible_user=ubuntu   # Name, IP, and SSH user for the target VM
